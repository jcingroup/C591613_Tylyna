@model IList<ProcCore.Business.DB0.Editor_L2>
@{
    ViewBag.Title = "ABOUT LITA";
    ViewBag.BodyClass = "AboutUs";
    Layout = "~/Views/Shared/Layout.cshtml";
}

@foreach (var i in Model)
{
    <section class="editor">
        @Html.Raw(i.l2_content)
    </section>
}
@if (Model.Count() <= 0)
{
    <section class="editor">
        @* 編輯器內容放此 *@
        Comming soon
    </section>
}
