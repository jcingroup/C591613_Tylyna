declare module "dt" {
    var twDistrict: Array<{ city: string, contain: { zip?: string, county?: string } }>;
    var dateFT: any;
    var PurchaseStateType: Array<server.StateTemplate>;
    var LangType: Array<server.StateTemplate>;
}
